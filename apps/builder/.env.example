PORT=3010
DATABASE_URL=postgres://devion_builder:change-me@localhost:5433/devion_builder
BUILDER_API_TOKEN=replace-with-at-least-24-random-characters
BUILDKIT_ADDRESS=tcp://localhost:1234
BUILDER_WORKDIR=/tmp/devion-builder
WORKER_POLL_MS=1000
WORKER_CONCURRENCY=2
# Optional: required only for workflow deploy steps.
FIRECRACKER_AGENT_URL=https://firecracker-host.example.internal:3020
# Comma-separated dashboard origins; leave empty to disable browser CORS.
CORS_ORIGINS=
