"use client";
import { useQuery } from "@tanstack/react-query";
import { Activity } from "lucide-react";
import { PageHeader } from "@/components/layout/page-header";
type Item = { id: string; type: string; description: string; createdAt: string };
export default function ActivitiesPage() { const { data = [], isLoading } = useQuery<Item[]>({ queryKey: ["personal", "activities"], queryFn: async () => { const response = await fetch(`${process.env.NEXT_PUBLIC_API_URL ?? ""}/api/personal/activities`, { credentials: "include" }); if (!response.ok) throw new Error("Aktivitäten konnten nicht geladen werden."); return response.json(); } }); return <div className="space-y-6 py-1"><PageHeader title="Meine Aktivitäten" description="Chronologische Übersicht deiner persönlichen Kontoaktivitäten." /><div className="divide-y divide-white/[0.06] overflow-hidden rounded-2xl border border-white/[0.07] bg-[#172128]/90">{data.map(item => <div className="flex gap-3 p-4" key={item.id}><Activity className="mt-0.5 size-4 text-[#81ecec]" /><div><p className="text-sm text-zinc-200">{item.description}</p><p className="mt-1 text-xs text-zinc-600">{item.type} · {new Intl.DateTimeFormat("de-DE", { dateStyle: "medium", timeStyle: "short" }).format(new Date(item.createdAt))}</p></div></div>)}{!isLoading && !data.length ? <p className="p-10 text-center text-sm text-zinc-500">Noch keine Aktivitäten vorhanden.</p> : null}</div></div>; }
